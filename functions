 write
 ft_printf
 signal : modifier le handler d'un signal
 sigemptyset : initialise un signal set vide. Tous les signaux sont exclus de ce set
 sigaddset : ajoute un signal au set
 sigaction : modifier le handler d'un signal (préférable de l'utiliser plutôt que signal() car propose plus de fonctionnalités
 et meilleure portabilité, le comportement de signal() peut différer d'un systène à l'autre)
 kill : envoie un signal au processus selon son PID
 getpid : retourne le PID du calling process
 malloc
 free
 pause : force le processus a sleep jusau'à réception d'un signal qui le termine ou lui fasse invoquer une fonction de gestionnaire de signal
 sleep : met le processus en sleep pendant x secondes
 usleep : met le processus en sleep pendant x microsecondes
 exit : permet de mettre fin au programme en spécifiant un code de retour